# 📝 Python Task Manager

A simple Command-Line Interface (CLI) application to manage daily tasks. This project uses Python and an SQLite database to perform CRUD operations (Create, Read, Update).

## 🚀 Features
- **Add New Task:** Save your tasks to the database.
- **View All Tasks:** See a list of all pending and completed tasks.
- **Mark Task as Done:** Update the status of a task to 'Completed'.
- **Persistent Storage:** Uses SQLite so your tasks are saved even after closing the program.

## 🛠️ Technologies Used
- **Language:** Python 3
- **Database:** SQLite3 (Built-in)

## 💻 How to Run the Project
1. Clone this repository:
   
   git clone [https://github.com/YourUsername/Python-Task-Manager.git](https://github.com/YourUsername/Python-Task-Manager.git)](https://github.com/naveenpython/Python-Task-Manager.git)
   Open the folder in your terminal.

Run the python file:

cmd 
python app.py
